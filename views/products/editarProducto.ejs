<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("../partials/head") %>
    <title><%= pageTitle  %> </title>
    <link rel="stylesheet" href="/css/agregarProducto.css" />
  </head>
  <body class="agregarProducto-body">
    <header><%- include ("../partials/header") %></header>
    <main class="agregarProducto-main-container">
      <form class="agregarProducto-form-container" action="/products/editar/<%= vino.id %>?_method=PUT" method="post" enctype="multipart/form-data">
        <div class="agregarProducto-div-boxes">
          <label for="nombre">Nombre</label>
          <% if (vino) {%> 
          <input
            class="agregarProducto-text-inputs"
            type="text"
            name="nombre"
            id="nombre"
            value= "<%= vino.nombre %>"
          />
          <% } %> 
        </div>
        <div class="agregarProducto-div-boxes">
          <label for="descripcionProducto">Bodega:</label>
          <% if (vino) {%> 
          <input
            class="agregarProducto-text-inputs"
            type="text"
            name="bodega"
            id="bodega"
            value= "<%= vino.bodega %>"
          />
          <% } %> 
        </div>
        <div class="agregarProducto-div-boxes">
          <label for="precioProducto">Precio:</label>
          <% if (vino) {%> 
          <input
            class="agregarProducto-text-inputs"
            type="number"
            name="precio"
            id="precio"
            value= "<%= vino.precio %>"
          />
          <% } %> 
        </div>
        <div class="agregarProducto-div-boxes">
          <label for="precioProducto">Descripcion:</label>
          <% if (vino) {%> 
          <textarea class="agregarProducto-text-area" name="descripcion" id="descripcion" cols="30" rows="10"><%= vino.descripcion %></textarea>
          <% } %> 
        </div>
        <div class="agregarProducto-div-boxes">
          <label for="imagenProducto">Imagen del producto: </label> 
          <input
            class="agregarProducto-text-inputs"
            type="file"
            accept="image/jpg"
            name="imagen"
            id="imagen"
            value="<%= vino.imagen %>"
          />
        </div>
        <div>
          <button
            class="agregarProducto-button"
            type="submit"
            value="Agregar producto"
          >
            Editar Producto
          </button>
        </div>
      </form>
    </main>
    <footer><%- include ("../partials/footer") %></footer>
  </body>
</html>
